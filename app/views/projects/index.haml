#content_articles
  - @projects.each do |project|
    %article
      %header
        %time.date
          =project.created_at.strftime('%d/%m/%Y')
        =link_to project.title, project
      .content
        = project.description
      %footer
  = paginate @projects